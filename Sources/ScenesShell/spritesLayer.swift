import Scenes

class SpritesLayer : Layer {
//    let sprites = Sprites()

    init() {
        // Using a meaningful name can be helpful for debugging
        super.init(name:"Sprites")

        // We insert our RenderableEntities in the constructor
  //      insert(entity:sprites, at:.front)
    }
}
